export type WidgetData = {
    widgetData: {
        outstandingOrderItems: number,
        uniqueProductsOrdered: number,
        totalAmountSpent: string,
        lateDeliveries: number
    }
} | any